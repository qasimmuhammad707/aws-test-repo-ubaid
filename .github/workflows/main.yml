script: |
  set -e

  cd $HOME

  if [ ! -d "ticketflow" ]; then
    git clone git@github.com:YOUR_USERNAME/YOUR_REPO.git ticketflow
  fi

  cd ticketflow

  git reset --hard
  git pull origin main

  docker compose down || true
  docker compose up --build -d
